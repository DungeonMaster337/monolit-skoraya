# Монолитная скорая помощь — лендинг

**Файл:** `index.html` — один файл, без сборки. Кидается на любой хостинг как есть.
**Стек:** статичный HTML + CSS, ~15 строк JS на отправку формы. Шрифты — Google Fonts (Oswald + Golos Text).

## Что сделано (25.09.2026)

Все 10 блоков из ТЗ клиента, тексты его, без изменений по смыслу.
Дизайн — под рабочий чертёж КЖ: размерные линии вместо разделителей, рамка листа
с полем подшивки в первом экране, красный «карандаш замечаний» как единственный
акцент, штамп чертежа вместо футера.

Проверено в живом Chrome:
- 1425px и 360px — горизонтального скролла нет;
- контраст всех текстовых пар ≥ 4.5:1 (замерено скриптом, не на глаз);
- шрифты грузятся, форма валидируется и показывает экран «принято»;
- есть мобильная панель вызова, `prefers-reduced-motion` уважается.

## Что ОБЯЗАТЕЛЬНО заполнить до публикации

1. **Телефон** — 6 мест, помечены в коде `TODO 1/6 … 6/6`. Поставить номер
   и `href="tel:+7XXXXXXXXXX"` (сейчас ссылки ведут на форму).
2. **Почта, название юрлица, ИНН/ОГРН** — в штампе внизу.
3. **Форма не подключена к почте.** Комментарий над `<form id="f">` — куда вписать
   Formspree / свой `send.php` / вебхук n8n. Пока кнопка только показывает «принято».
4. **Политика обработки ПДн** — две ссылки `href="#"` (в форме и в футере).
   Без реального документа чекбокс согласия юридически пустой (152-ФЗ).
5. **Кейсы** — клиент прислал их как шаблон. Заменить на реальные объекты и цифры.

## Что улучшит конверсию, но требует материалов от клиента

- 4–6 фото с объектов (опалубка, каркас, залитая плита, бригада в работе) —
  сейчас страница целиком типографская, фото добавят доверия.
- Реквизиты и скан СРО/допусков, если есть.
